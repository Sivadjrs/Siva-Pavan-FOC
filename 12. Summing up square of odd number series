#include <stdio.h>

int main() {

    int n;

    printf("Enter the number of terms: ");

    scanf("%d", &n);

    

    int sum = n * (2*n - 1) * (2*n + 1) / 3;

    printf("The sum of the squares of the first %d odd numbers is %d\n", n, sum);

    

    return 0;

}
